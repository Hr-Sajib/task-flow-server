import express from "express";
import { authServices } from "./auth.service";
const router = express.Router();

// router.post("/login", authServices.loginUserIntoDB);


export const AuthRoutes = router;